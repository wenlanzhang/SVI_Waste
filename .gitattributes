# Git LFS — only for files that must be in the repo and exceed GitHub's 100 MB limit.
# Prefer keeping data & models out of git entirely (see .gitignore).

# If you ever need to track a specific large CSV or model, add it here:
# Data/Output/example.csv filter=lfs diff=lfs merge=lfs -text
# models/waste_0315.pt filter=lfs diff=lfs merge=lfs -text
